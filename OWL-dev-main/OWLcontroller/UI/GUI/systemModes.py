from enum import Enum
class systemExecutionModes(Enum):  #TODO  look at this -> need to move this from the GUI to be near the controlPC in the folders
    LEGACY_MODE_EXCERCISER = 1
    LEGACY_MODE_HOST_PC = 2